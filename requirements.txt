# Core FastAPI + ASGI
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
starlette>=0.37.0

# Pydantic / settings
pydantic>=2.8.0
pydantic-settings>=2.4.0
pydantic-core>=2.18.0

# Database
sqlalchemy[asyncio]>=2.0.30
aiosqlite>=0.20.0

# Auth / JWT / crypto
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
cryptography>=43.0.0

# Logging
structlog>=24.2.0

# HTTP client
httpx>=0.27.0
python-multipart>=0.0.9

# Alembic (migrations, lightweight)
alembic>=1.13.0

# WebSockets
websockets>=12.0
